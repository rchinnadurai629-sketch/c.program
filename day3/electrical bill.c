#include <stdio.h>
int main() {
    int units;
    printf("units:");
    scanf("%d",&units);
    if (units=units*5) {
        printf("Bill amount = Rs.%d",units);
    }
    else {
        printf("Bill amount = Rs.%d",units);
    }
    return 0;
}